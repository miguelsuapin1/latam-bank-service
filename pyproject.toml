[project]
name = "latam-bank-pipeline"
version = "0.1.0"
description = "Data pipeline, analysis and evaluation for the LATAM bank service"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1",
    "pandas>=2.2",
    "pyarrow>=17",
    "tabulate>=0.9",
]

[tool.uv]
package = false
